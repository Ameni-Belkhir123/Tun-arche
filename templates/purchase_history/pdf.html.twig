<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Purchase History</title>
    <style>
        /* General page styles */
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
            color: #333;
        }

        /* Header / Logo section */
        header {
            text-align: center;
            margin-bottom: 30px;
        }
        header img {
            max-height: 60px; /* Adjust as needed for your logo */
        }

        /* Event / Title block */
        .event-info {
            text-align: center;
            margin-bottom: 20px;
        }
        .event-info h2 {
            margin: 0;
            font-size: 28px;
        }

        /* Date/time stamp (optional) */
        .generation-date {
            text-align: right;
            font-size: 14px;
            color: #888;
        }

        /* Table styling */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
            vertical-align: middle;
        }
        th {
            background-color: #f5f5f5;
            font-weight: 600;
        }
        /* Alternating row colors */
        tbody tr:nth-child(even) {
            background-color: #fafafa;
        }

        /* Optional subtitle style if needed */
        h2.subtitle {
            margin-top: 20px;
            font-size: 18px;
            text-transform: uppercase;
            color: #777;
        }
    </style>
</head>
<body>

<div class="event-info">
    <h2>My Purchase History</h2>
    <!-- Example date/time generation (set this dynamically in your controller if you want) -->
    <div class="generation-date">
        Generated on: {{ "now"|date("Y-m-d H:i") }}
    </div>
</div>

<table>
    <thead>
    <tr>
        <th>Ticket Number</th>
        <th>Date of Emission</th>
        <th>Payment Mode</th>
        <th>Ticket Type</th>
        <th>Event</th>
    </tr>
    </thead>
    <tbody>
    {% for ticket in tickets %}
        <tr>
            <td>{{ ticket.numero }}</td>
            <td>{{ ticket.dateEmission ? ticket.dateEmission|date('Y-m-d') : '' }}</td>
            <td>{{ ticket.modePaiement }}</td>
            <td>{{ ticket.type }}</td>
            <td>{{ ticket.event ? ticket.event.nameEvent : 'N/A' }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>
