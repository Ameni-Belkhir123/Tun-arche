{% extends "back/base.html.twig"%}
{% block body %}
    <h1>Commantaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commantaire.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commantaire.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_com_index') }}" class="btn btn-secondary">
    <i class="fas fa-arrow-left"></i> Retour à la liste
</a>

<a href="{{ path('app_com_edit', {'id': commantaire.id}) }}" class="btn btn-warning">
    <i class="fas fa-edit"></i> Modifier
</a>


    {{ include('com/_delete_form.html.twig') }}
{% endblock %}
